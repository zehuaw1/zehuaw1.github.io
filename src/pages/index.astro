---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionHeading from '../components/SectionHeading.astro';
import ProjectCard from '../components/ProjectCard.astro';
import FeaturedLead from '../components/FeaturedLead.astro';
import HeroAnimation from '../components/HeroAnimation.tsx';
import ScrollFadeIn from '../components/ScrollFadeIn.tsx';
import siteConfig from '../data/site.config';
import projects from '../data/projects';
import { getSocialIcon } from '../utils/helpers';

const featuredProjects = projects.filter((p) => p.featured).slice(0, 3);
const [leadProject, ...followerProjects] = featuredProjects;

// Get last git commit date at build time
let lastUpdated = new Date().toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric', timeZone: 'America/New_York' });
try {
  const { execSync } = await import('child_process');
  const gitDate = execSync('git log -1 --format=%ci', { encoding: 'utf-8' }).trim();
  if (gitDate) {
    lastUpdated = new Date(gitDate).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric', timeZone: 'America/New_York' });
  }
} catch {};
---

<BaseLayout personSchema={true}>
  <section class="hero-section">
    <span class="hero-watermark" aria-hidden="true">∇</span>

    <div class="hero-container">
      <div class="hero-layout">
        <div class="hero-content">
          <HeroAnimation
            client:load
            name={siteConfig.name}
            tagline={siteConfig.tagline}
            highlightWords={siteConfig.highlightWords}
            tags={siteConfig.heroTags}
          />

          <div class="hero-actions">
            <a href="#about" class="hero-link">
              About me
              <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14m-7-7 7 7-7 7"/></svg>
            </a>
            <a href="/cv" class="hero-link">CV</a>
          </div>

          <div class="hero-socials">
            {siteConfig.socials.map((social) => (
              <a
                href={social.url}
                aria-label={social.label}
                class="hero-social-link"
                target={social.url.startsWith('mailto:') ? undefined : '_blank'}
                rel={social.url.startsWith('mailto:') ? undefined : 'noopener noreferrer'}
              >
                <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
                  <path d={getSocialIcon(social.platform)} />
                </svg>
              </a>
            ))}
          </div>
        </div>

        {siteConfig.profileImage && (
          <div class="hero-pfp">
            <img
              src={siteConfig.profileImage}
              alt={siteConfig.name}
              class="hero-pfp-img"
              loading="eager"
            />
          </div>
        )}
      </div>
    </div>
  </section>

  <hr class="section-divider" />

  <section class="section" id="about">
    <div class="container">
      <ScrollFadeIn client:visible>
        <SectionHeading title="About" index="01" />
      </ScrollFadeIn>
      <ScrollFadeIn client:visible delay={0.1}>
        <div class="about-full">
          <div class="about-bio prose-content">
            {siteConfig.bio.split('\n\n').map((paragraph) => (
              <p set:html={paragraph.trim()} />
            ))}
          </div>
        </div>
      </ScrollFadeIn>
      <ScrollFadeIn client:visible delay={0.15}>
        <div class="about-interests-section">
          <h3 class="subsection-title">Research Interests</h3>
          <p class="interests-line">
            {siteConfig.researchInterests.map((interest, i) => (
              <>
                <span class="interest-item">{interest}</span>
                {i < siteConfig.researchInterests.length - 1 && (
                  <span class="interest-sep" aria-hidden="true"> · </span>
                )}
              </>
            ))}
          </p>
        </div>
      </ScrollFadeIn>
      {siteConfig.beyondAcademics && (
        <ScrollFadeIn client:visible delay={0.2}>
          <div class="about-beyond">
            <h3 class="subsection-title">Beyond Research</h3>
            <div class="prose-content">
              {siteConfig.beyondAcademics.split('\n').map((line) => (
                <p>{line.trim()}</p>
              ))}
            </div>
          </div>
        </ScrollFadeIn>
      )}
      <ScrollFadeIn client:visible delay={0.25}>
        <div class="about-contact">
          <p class="prose-content">I'm always happy to chat, whether it's about research, physics, AI, or anything else. If something here caught your interest, feel free to <a href={`mailto:${siteConfig.email}`}>reach out</a>.</p>
        </div>
      </ScrollFadeIn>
    </div>
  </section>


  <hr class="section-divider" />

  <section class="section">
    <div class="container">
      <ScrollFadeIn client:visible>
        <SectionHeading
          title="Featured Projects"
          subtitle="Selected research projects and open-source work"
          index="02"
        />
      </ScrollFadeIn>
      <div class="featured-stack">
        {leadProject && (
          <ScrollFadeIn client:visible>
            <FeaturedLead project={leadProject} />
          </ScrollFadeIn>
        )}
        {followerProjects.length > 0 && (
          <div class="followers-grid">
            {followerProjects.map((project, i) => (
              <ScrollFadeIn client:visible delay={(i + 1) * 0.08}>
                <ProjectCard project={project} />
              </ScrollFadeIn>
            ))}
          </div>
        )}
      </div>
      <ScrollFadeIn client:visible>
        <div class="section-cta">
          <a href="/projects" class="text-link">View all projects →</a>
        </div>
      </ScrollFadeIn>
    </div>
  </section>

  <hr class="section-divider" />

  <section class="section">
    <div class="container">
      <ScrollFadeIn client:visible>
        <SectionHeading title="News" subtitle="Recent updates and announcements" index="03" />
      </ScrollFadeIn>
      <div class="news-list">
        {siteConfig.news.map((item, i) => (
          <ScrollFadeIn client:visible delay={i * 0.08}>
            <div class="news-item">
              <span class="news-glyph" aria-hidden="true">∇</span>
              <span class="news-date">{item.date}</span>
              <p class="news-text">
                {item.link ? (
                  <a href={item.link}>{item.text}</a>
                ) : (
                  item.text
                )}
              </p>
            </div>
          </ScrollFadeIn>
        ))}
      </div>
    </div>
  </section>

  <div class="last-updated">
    Last updated {lastUpdated}
  </div>
</BaseLayout>

<style>
  .hero-section {
    min-height: calc(100vh - 5rem);
    display: flex;
    align-items: center;
    padding: 4rem 0 5rem;
    position: relative;
  }

  @media (min-width: 768px) {
    .hero-section {
      padding: 5rem 0 7rem;
    }
  }

  .hero-watermark {
    position: absolute;
    top: 50%;
    left: clamp(1.5rem, 5vw, 4rem);
    transform: translateY(-50%);
    font-family: var(--font-serif);
    font-style: italic;
    font-weight: 500;
    font-size: clamp(10rem, 22vw, 20rem);
    line-height: 1;
    color: var(--accent);
    opacity: 0.05;
    pointer-events: none;
    user-select: none;
    z-index: 0;
  }

  .hero-container {
    position: relative;
    z-index: 1;
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
    width: 100%;
  }

  .hero-layout {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 4rem;
  }

  .hero-content {
    max-width: 48rem;
    flex: 1;
  }

  .hero-pfp {
    flex-shrink: 0;
    display: none;
  }

  @media (min-width: 768px) {
    .hero-pfp {
      display: block;
    }
  }

  .hero-pfp-img {
    width: 360px;
    height: 360px;
    border-radius: 50%;
    object-fit: cover;
    box-shadow: 0 8px 30px oklch(15% 0.01 35 / 0.12), 0 2px 8px oklch(15% 0.01 35 / 0.06);
    border: 3px solid var(--bg);
    outline: 1px solid var(--border);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .hero-pfp-img:hover {
    transform: translateY(-6px);
    box-shadow: 0 16px 40px oklch(15% 0.01 35 / 0.16), 0 6px 16px oklch(15% 0.01 35 / 0.08);
  }

  .hero-actions {
    display: flex;
    gap: 1.5rem;
    margin-top: 2.5rem;
    flex-wrap: wrap;
    align-items: center;
  }

  .hero-link {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    font-size: 0.9rem;
    font-weight: 500;
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.15s;
  }

  .hero-link:hover {
    color: var(--text-primary);
  }

  .hero-socials {
    display: flex;
    gap: 1rem;
    margin-top: 2.5rem;
  }

  .hero-social-link {
    color: var(--text-muted);
    transition: color 0.15s;
    text-decoration: none;
  }

  .hero-social-link:hover {
    color: var(--text-primary);
  }

  .container {
    max-width: 72rem;
    margin: 0 auto;
    padding: 0 1.5rem;
  }

  .about-full {
    max-width: 48rem;
  }

  .about-bio {
    margin-bottom: 2.5rem;
  }

  .about-interests-section {
    margin-top: 2rem;
    margin-bottom: 2rem;
  }

  .subsection-title {
    font-size: 0.88rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: 1rem;
    color: var(--text-muted);
  }

  .interests-line {
    font-size: 1.05rem;
    line-height: 1.7;
    color: var(--text-primary);
    margin: 0;
    max-width: 56ch;
  }

  .interest-item {
    font-weight: 500;
  }

  .interest-sep {
    color: var(--text-muted);
    font-weight: 400;
    margin: 0 0.15em;
  }

  .about-beyond {
    margin-top: 2rem;
  }

  .about-contact {
    margin-top: 2.5rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border-subtle);
  }

  .about-contact p {
    font-style: italic;
    color: var(--text-muted);
  }

  .text-link {
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.15s;
  }

  .text-link:hover {
    color: var(--text-primary);
  }

  .featured-stack {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .followers-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 1.25rem;
  }

  .section-cta {
    margin-top: 2.5rem;
    text-align: center;
  }

  .news-list {
    max-width: 48rem;
    display: flex;
    flex-direction: column;
  }

  .news-item {
    display: grid;
    grid-template-columns: 1.25rem 5.5rem 1fr;
    gap: 0.75rem 1rem;
    padding: 0.85rem 0;
    border-bottom: 1px solid var(--border-subtle);
    align-items: baseline;
  }

  @media (max-width: 640px) {
    .news-item {
      grid-template-columns: 1.25rem 1fr;
      gap: 0.5rem 0.75rem;
    }
    .news-date {
      grid-column: 2;
    }
    .news-text {
      grid-column: 1 / -1;
      padding-left: 2rem;
    }
  }

  .news-item:last-child {
    border-bottom: none;
  }

  .news-glyph {
    font-family: var(--font-serif);
    font-style: italic;
    font-size: 1rem;
    color: var(--accent);
    line-height: 1;
    align-self: center;
    text-align: center;
  }

  .news-date {
    font-size: 0.78rem;
    font-weight: 500;
    font-family: var(--font-mono);
    color: var(--text-muted);
    white-space: nowrap;
  }

  .news-text {
    font-size: 0.9rem;
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
  }

  .news-text a {
    color: var(--text-secondary);
    text-decoration: none;
    transition: color 0.15s;
  }

  .news-text a:hover {
    color: var(--text-primary);
  }

  .last-updated {
    text-align: center;
    padding: 2rem 1.5rem 0;
    font-size: 0.78rem;
    color: var(--text-muted);
    font-family: var(--font-mono);
  }
</style>
